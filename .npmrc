# Next/Tailwind/esbuild need their postinstall scripts to run
enable-pre-post-scripts=true
# Keep phantom deps from compiling — every import must be a declared dependency
strict-peer-dependencies=false
auto-install-peers=true
